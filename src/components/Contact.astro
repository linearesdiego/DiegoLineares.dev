---
import { Image } from "astro:assets";
import gmail from "../../public/images/icons/gmail.svg";
import copy from "../../public/images/icons/copy.svg";
import likedin from "../../public/images/icons/linkedin.svg";
import SociaIcons from "./SociaIcons.astro";
---

<section id="contact" class="w-full bg-secondary">
  <div class="containerWidth">
    <div class="py-20">
      <div class="w-full flex flex-col items-center gap-4 justify-center">
        <span class="rounded-xl bg-[#374151] px-5 py-1">Contacto</span>

        <p class="text-gray-300 text-xl">
          No dude en comunicarse conmigo si está buscando un desarrollador,
          tiene alguna consulta o simplemente desea conectarse.
        </p>
      </div>
      <div class="w-full flex flex-col items-center gap-4 justify-center mt-10">
        <div class="flex gap-1 lg:gap-5 items-center">
          <Image
            src={gmail}
            width={40}
            height={40}
            class="w-4 lg:w-[50px]"
            alt="gmail"
          />
          <span class="text-white text-xl lg:text-3xl font-bold"
            >diegolineares@gmail.com</span
          >
          <Image
            id="gmail"
            src={copy}
            width={40}
            height={40}
            alt="gmail"
            class="hover:bg-tertiary rounded-lg p-1.5 transition-colors duration-200 cursor-pointer w-8 lg:w-[50px]"
          />
        </div>

        <div class="flex flex-col gap-3 items-center">
          <p class="text-gray-300">
            ¡También puedes encontrarme en estas plataformas!
          </p>

          <SociaIcons />
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  document.getElementById("gmail")?.addEventListener("click", () => {
    navigator.clipboard.writeText("diegolineares@gmail.com");
    alert("Correo copiado al portapapeles");
  });
</script>
